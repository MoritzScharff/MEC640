def fun_test(x: int) -> int:
    """Use the test fun to check how Sphinx and Python work.

        :param x: x is an input variable
        :type x: int
        :return: this is the output of the function
        :rtype: int

        .. note::

            This can be used to highlight anything...

        """
    z = x + 1
    return z
